#!/usr/bin/env node

console.error("CLI 승인은 비활성화되어 있습니다. 대시보드에서 먼저 구성을 승인한 뒤, 현재 Sonol 터미널 세션에서 `승인`을 입력하세요.");
console.error("CLI approval is disabled. Approve the plan in the dashboard first, then type `승인` in the current Sonol terminal session.");
process.exit(1);
